// Blank Ember shell — the single placeholder route (index `/`).
// Real routing and views land with feature work.
export default function App() {
  return (
    <main>
      <h1>Ember</h1>
    </main>
  );
}
